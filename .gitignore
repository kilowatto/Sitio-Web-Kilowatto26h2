# build output
dist/
# generated types
.astro/
# wrangler local state/cache
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.dev.vars
.dev.vars.*

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code session state
.claude/scheduled_tasks.lock

# raw/unreviewed personal photo staging - never commit (unprocessed, potential PII, this repo is public)
fotos temporales/
fotos temporales 2/

# Remotion: dependencias pesadas y la copia del navegador headless
remotion/node_modules/
remotion/out/

# Remotion's webpack bundle, rebuilt inside the container image on every build.
remotion/build/
